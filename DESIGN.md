# 馬上槍試合 — 設計

## フロー（約12秒/試合）

```
ルーム作成 / 参加
  → 競技場画面 + READY（両者）
  → 3,2,1 カウントダウン（3秒）
  → 突撃（5秒）— 上下ドラッグで槍の高さ
  → 判定表示 → 勝敗 → 再戦
```

## 操作

試合中の操作は **画面上下ドラッグ** のみ。槍の高さを調整し、衝突時に PERFECT ゾーンへ合わせる。

## 判定

| ゾーン | 高さ (0=LOW, 1=HIGH) | 結果 |
|--------|---------------------|------|
| PERFECT | 0.42〜0.58 | 最強 |
| GOOD | 0.28〜0.72（PERFECT外） | 通常 |
| MISS | それ以外 | 空振り |

勝敗は両者の tier スコア比較（PERFECT=3, GOOD=2, MISS=0）。

## 同期フィールド

- `ready`
- `matchStartTime`
- `lanceHeight`（120ms間隔、座標は同期しない）
- `impactResult`
- `rematchRequest`

馬の位置は `matchStartTime` からクライアント側で決定論的に計算。
